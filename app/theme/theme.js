import {Dimensions} from 'react-native';
const {height, width} = Dimensions.get('window');

export const mainColor = '#0e76ee';
export const SCREEN_HEIGHT = height;
export const SCREEN_WIDTH = width;
