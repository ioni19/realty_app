import React from "react";
import {Text} from "react-native";
import {useNavigation} from "@react-navigation/native";

const Contact = () => {
  const navigation = useNavigation();

  return <Text>컨택트</Text>;
};

export default Contact;
